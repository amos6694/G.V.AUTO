kind = "web"
previewPath = "/"
title = "File Fingerprint"
version = "1.0.0"
id = "artifacts/file-fingerprint"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19064

[services.development]
run = "pnpm --filter @workspace/file-fingerprint run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/file-fingerprint", "run", "build" ]
publicDir = "artifacts/file-fingerprint/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19064"
BASE_PATH = "/"
